wordpress:
  image: bitnami/wordpress:latest
  service:
    type: ClusterIP
    port: 80
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"
  livenessProbe:
    httpGet:
      path: /wp-admin/install.php
      port: 8080
    initialDelaySeconds: 600
    periodSeconds: 30
    timeoutSeconds: 10
    failureThreshold: 3
  readinessProbe:
    httpGet:
      path: /
      port: 8080
    initialDelaySeconds: 180
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6
  adminUser: "admin"
  adminEmail: "admin@example.com"
  blogName: "WooCommerce Store"
  
mysql:
  image: mysql:8.0
  auth:
    database: wordpress_db
    username: wordpress_user
  
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"
  persistence:
    size: 1Gi
    # storageClass: "standard"

ingress:
  enabled: true
  className: "nginx"
  host: "store.local"

  
